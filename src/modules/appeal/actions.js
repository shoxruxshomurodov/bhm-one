import { createRoutine } from 'redux-saga-routines';

const GET_APPEAL = createRoutine("GET_APPEAL");
const SET_APPEAL = createRoutine("SET_APPEAL");
export default {
    GET_APPEAL,
    SET_APPEAL,
}